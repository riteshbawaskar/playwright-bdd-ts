{
  "name": "playwright-bdd-ui-validator-ts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "cucumber-js",
    "start:demo": "node demo/server.js",
    "install-browsers": "npx playwright install --with-deps || npx playwright install",
    "prepare": "npm run install-browsers"
  },
  "dependencies": {
    "@cucumber/cucumber": "11.3.0",
    "@playwright/test": "1.56.0",
    "dotenv": "^16.4.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "^5.6.3"
  },
  "type": "module"
}